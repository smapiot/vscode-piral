<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,minimum-scale=1,user-scalable=no">
  <% styles.forEach(function(style){ %>
     <link rel="stylesheet" href="<%= style %>" />
  <% }); %>
</head>

<body>
  <div id="root">
    <div class="page">
      <div class="header" x-memo>
        <h1 class="title">Create Project</h1>
        <p class="subTitle">
          Create a new Piral instance or a pilet for an existing instance.
        </p>
      </div>
      <div class="container">
        <div class="cards containerColumn">
          <p class="columnTitle">Select Type <span class="errorRepoType errorMessage hide">[Required]</span></p>
        <% repoTypes.forEach((repoType, index) => { %>
          <div
            key="repoType<%= index %>"
            class="card"
            repoType="<%= repoType.type %>"
            >
            <img class="selectedCardTag" src="<%= images.selectedItemIcon %>" />
            <div class="cardTitle">
              <img class="cardTitleIcon" src="<%= repoType.icon %>" />
              <p class="cardTitleTxt"><%= repoType.title %></p>
            </div>
            <p class="cardDescription"><%= repoType.description %></p>
          </div>
        <% }); %>
        </div>
        <div class="information containerColumn">
          <p class="columnTitle">Provide Information</p>
          <div class="extraItem">
            <p class="extraItemLabel">Name <span class="errorName errorMessage hide">[required]</span></p>
            <input
              class="extraItemInput"
              stateName="name"
            >
          </div>
          <div class="extraItem">
            <p class="extraItemLabel">Version <span class="errorVersion errorMessage hide">[required]</span></p>
            <input
              class="extraItemInput"
              stateName="version"
              >
          </div>
          <div class="extraItem">
            <p class="extraItemLabel">Description <span class="errorDescription errorMessage hide">[required]</span></p>
            <textarea
              class="extraItemInput"
              stateName="description"
              ></textarea>
          </div>
          <div class="extraItem onlyForPilet hide">
            <p class="extraItemLabel">Piral Package <span class="errorPiralPackage errorMessage hide">[required]</span></p>
            <input
              class="extraItemInput"
              stateName="piralPackage"
              >
          </div>
          <div class="extraItem onlyForPilet hide">
            <p class="extraItemLabel">NPM Registry <span class="errorNpmRegistry errorMessage hide">[required]</span></p>
            <input
              class="extraItemInput"
              stateName="npmRegistry"
              >
          </div>
        </div>
        <div class="cards containerColumn">
          <p class="columnTitle">Select bundler <span class="errorBundler errorMessage hide">[required]</span></p>
          <% bundlers.forEach((bundler, index) => { %>
            <div
              key="bundler_<%= index %>"
              class="bundlerItem"
              bundler="<%= bundler.type %>"
              >
              <img class="selectedCardTag" src="<%= images.selectedItemIcon %>" />
              <div class="cardTitle">
                <img class="cardTitleIcon" src="<%= bundler.icon %>" />
                <p class="cardTitleTxt"><%= bundler.title %></p>
              </div>
              <div class="cardDescription"><%= bundler.description %></div>
            </div>
          <% }); %>
        </div>
      </div>
    </div>
    <button class="btn create">Scaffold Project</button>
  </div>
  <% scripts.forEach((script) => { %>
     <script src="<%= script %>"></script>
  <% }); %>
</body>

</html>
